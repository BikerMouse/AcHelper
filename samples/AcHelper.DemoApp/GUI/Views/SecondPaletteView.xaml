<UserControl x:Class="AcHelper.DemoApp.GUI.Views.SecondPaletteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AcHelper.DemoApp.GUI.Views"
             xmlns:theme="clr-namespace:AcHelper.WPF.Themes;assembly=AcHelper.WPF"
             mc:Ignorable="d" 
             theme:PaletteControlExtension.Theme="{Binding ThemeManager.CurrentTheme}"
             DataContext="{Binding Main, Source={StaticResource Locator}}"
             d:DesignHeight="300" d:DesignWidth="300">
    <!--<UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/AcHelper.DemoApp;component/Themes/LightTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>-->
    <Grid Background="Red">
        <Border Style="{DynamicResource ModuleBorder}">
            <TextBlock Text="Second palette"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="28"
                       Foreground="Red"/>
        </Border>
    </Grid>
</UserControl>
